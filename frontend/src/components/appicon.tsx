import { Image } from "@heroui/react";
import React from "react";
import url from "@/assets/images/appicon.png";

const AppIcon = () => {
  return <Image src={url} width={40} height={40} alt="BSM370 Interface Logo" />;
};

export default AppIcon;
